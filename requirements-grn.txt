# macOS + pip DGL: GraphBolt ships libgraphbolt up to PyTorch 2.3.x; torchdata>=0.11 drops datapipes
# (needed by DGL graphbolt). Pin these together before: pip install -r requirements-grn.txt
torch==2.3.0
torchvision==0.18.0
torchdata==0.8.0
dgl==2.2.1
numpy
pandas
scikit-learn

# Optional: scripts/encode_gene_symbols.py (gene symbol embeddings)
# transformers>=4.36
